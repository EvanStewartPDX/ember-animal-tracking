
import { userTile } from 'ember-animal-tracking/helpers/user-tile';
import { module, test } from 'qunit';

module('Unit | Helper | user tile');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userTile([42]);
  assert.ok(result);
});

